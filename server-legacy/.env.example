# ── server-legacy: Local development OAuth proxy only ───────────────────────
# This Express server is NOT used in production (Vercel uses api/ serverless functions).
# Run with: npm run start (from project root) or cd server-legacy && npm start

RAINDROP_CLIENT_ID=your_client_id_here
RAINDROP_CLIENT_SECRET=your_client_secret_here
FRONTEND_URL=http://localhost:5173
PORT=3001
